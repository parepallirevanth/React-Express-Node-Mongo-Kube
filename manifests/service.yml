apiVersion: v1
kind: Service
metadata:
    name: parepallirevanthreactexpressnodemongokube
spec:
    type: LoadBalancer
    ports:
    - port: 8080 
    selector:
        app: parepallirevanthreactexpressnodemongokube